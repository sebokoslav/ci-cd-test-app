apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cicdtest-argo-application-develop
  namespace: cicdtest-develop
spec:
  project: default

  source:
    repoURL: https://github.com/sebokoslav/ci-cd-test-app.git
    targetRevision: develop
    path: config
  destination: 
    server: https://kubernetes.default.svc
    namespace: cicdtest-develop

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cicdtest-deployment-develop
  labels:
    app: cicdtest-develop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cicdtest-develop
  template:
    metadata:
      labels:
        app: cicdtest-develop
    spec:
      containers:
      - name: cicdtest-develop
        image: sebokoslav/ci-cd-test-app:v1.0.16
        ports:
        - containerPort: 8082
---
apiVersion: v1
kind: Service
metadata:
  name: cicdtest-service-develop
spec:
  type: LoadBalancer
  selector:
    app: cicdtest-develop
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 80
